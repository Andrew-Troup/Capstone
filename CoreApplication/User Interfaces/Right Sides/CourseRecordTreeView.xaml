    <UserControl x:Class="CoreApplication.User_Interfaces.Right_Sides.CourseRecordTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoreApplication.User_Interfaces.Right_Sides"
             xmlns:handler="clr-namespace:CoreApplication.ModelHandlers.Records"
             xmlns:support="clr-namespace:CoreApplication.Models.Records.Class.Support"
             mc:Ignorable="d" 
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <DockPanel>
        <Border BorderThickness="5">
            <TreeView x:Name="classTreeView" SelectedItemChanged="classTreeView_SelectedItemChanged" Background="AliceBlue">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type handler:ClassRecordHandler}" ItemsSource="{Binding Classes}">
                        <StackPanel Orientation="Horizontal">
                            <Border BorderBrush="Black" BorderThickness="1">
                                <StackPanel Orientation="Horizontal">
                                    <Border BorderThickness="5">
                                        <TextBlock Text="Class Name:"/>
                                    </Border>

                                    <Border BorderThickness="5">
                                        <TextBlock Text="{Binding ClassName}"/>
                                    </Border>

                                    <Border BorderThickness="5">
                                        <TextBlock Text="Class ID:"/>
                                    </Border>

                                    <Border BorderThickness="5">
                                        <TextBlock Text="{Binding ClassID}"/>
                                    </Border>

                                    <Border BorderThickness="5">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text=" [" Foreground="Blue" />
                                            <TextBlock Text="{Binding StudentRecords.Count}" Foreground="Blue" />
                                            <TextBlock Text="]" Foreground="Blue" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </HierarchicalDataTemplate>
                    <DataTemplate DataType="{x:Type support:DepartmentCourseInformationsPair}">
                        <StackPanel Orientation="Horizontal">
                            <Border BorderBrush="Black" BorderThickness="1">
                                <StackPanel Orientation="Horizontal">
                                    <Border BorderThickness="2" Width="Auto">
                                        <TextBlock Text="First Name: "/>
                                    </Border>

                                    <Border BorderThickness="2">
                                        <TextBlock Text="{Binding PersonalInformation.FirstName}" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <Border BorderThickness="2" Width="Auto">
                                        <TextBlock Text="Last Name: "/>
                                    </Border>

                                    <Border BorderThickness="2" Width="Auto">
                                        <TextBlock Text="{Binding PersonalInformation.LastName }" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <Border BorderThickness="2" Width="Auto">
                                        <TextBlock Text="Student ID: "/>
                                    </Border>

                                    <Border BorderThickness="2" Width="Auto">
                                        <TextBlock Text="{Binding PersonalInformation.ID}" Width="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Border>
    </DockPanel>
</UserControl>
